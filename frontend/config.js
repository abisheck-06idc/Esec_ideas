// Deployment configuration.
// For local development the backend runs on localhost.
// Before production deployment, replace this URL with your Render backend URL,
// for example: https://your-backend.onrender.com/api
window.APP_CONFIG = {
    API_BASE_URL: "https://esec-ideas.onrender.com/api"
};
